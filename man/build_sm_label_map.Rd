% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_sm_label_map.R
\name{build_sm_label_map}
\alias{build_sm_label_map}
\title{Build a select_multiple label map from an XLSForm}
\usage{
build_sm_label_map(
  tool,
  excluded_cols = "",
  choice_label = NULL,
  tool_flavor = "auto"
)
}
\arguments{
\item{tool}{Path to the XLSForm, a \code{\link[=read_xlsform]{read_xlsform()}} result list, or a
pre-read \code{read_xlsform()} result.}

\item{excluded_cols}{Vector of question names to skip.}

\item{choice_label}{Column name in the \code{choices} sheet that holds the
choice label. If \code{NULL} (default), the function tries the typical
XLSForm forms (\code{label::English}, \code{label:English}, \code{label}) and picks
the first one present.}

\item{tool_flavor}{One of \code{"auto"}, \code{"kobo"}, \code{"surveycto"}. Only used
when \code{tool} is a file path. Default \code{"auto"}.}
}
\value{
Data frame with columns \code{Question}, \code{type}, \code{select_type},
\code{response_code}, \code{response_label}, \code{response_label_new}, \code{dataset_col},
\code{labeled_col}. \code{labeled_col} is \code{"<Question>/<sanitized_label>"}.
}
\description{
Reads an XLSForm and returns a data frame that maps each raw dataset
column for a \code{select_multiple} question (e.g. \code{q1_3}) to a labeled
equivalent (e.g. \code{q1/Banana}). The choice label is sanitized to be safe
inside an R variable name - characters like \code{/}, \verb{'}, \code{(}, \verb{\%}, and
whitespace are replaced with \verb{_}. Works with both Kobo and SurveyCTO.
}
\details{
The output is the input expected by \code{\link[=apply_sm_label_map]{apply_sm_label_map()}}.
}
