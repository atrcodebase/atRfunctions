% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_xlsform.R
\name{read_xlsform}
\alias{read_xlsform}
\title{Read an XLSForm (Kobo or SurveyCTO) into a normalized list}
\usage{
read_xlsform(tool, flavor = c("auto", "kobo", "surveycto"), guess_max = 1e+05)
}
\arguments{
\item{tool}{Path to the XLSForm \code{.xlsx} file.}

\item{flavor}{One of \code{"auto"}, \code{"kobo"}, or \code{"surveycto"}. When \code{"auto"} (the
default) the flavor is detected from the column names actually present in
the file.}

\item{guess_max}{Passed to \code{\link[readxl:read_excel]{readxl::read_excel()}}. Defaults to \code{100000}.}
}
\value{
A list with elements \code{survey} (data frame, normalized), \code{choices}
(data frame, normalized), and \code{flavor} (the detected or supplied flavor).
}
\description{
Reads the \code{survey} and \code{choices} sheets of an XLSForm and normalizes the
column names so downstream code does not have to care whether the tool was
designed in Kobo or SurveyCTO. Specifically: \code{relevance} is renamed to
\code{relevant}, \verb{list name} to \code{list_name}, and \code{value} to \code{name}.
}
