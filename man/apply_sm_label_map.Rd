% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_sm_label_map.R
\name{apply_sm_label_map}
\alias{apply_sm_label_map}
\title{Rename select_multiple series columns to use response labels}
\usage{
apply_sm_label_map(
  data,
  x,
  question_separator = "_",
  sm_label_separator = ".",
  excluded_cols = "",
  choice_label = NULL,
  tool_flavor = "auto"
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{Either a mapping data frame (must have \code{dataset_col} and
\code{labeled_col} columns) or an XLSForm (path, \code{\link[=read_xlsform]{read_xlsform()}} result,
or \code{survey} data frame). The two cases are distinguished by the
presence of \code{dataset_col}/\code{labeled_col} columns.}

\item{question_separator}{Separator between question name and choice
code in the dataset columns. Default \code{"_"}.}

\item{sm_label_separator}{Forwarded to \code{\link[=build_sm_label_map]{build_sm_label_map()}} when \code{x}
is an XLSForm. Default \code{"."}. Ignored when \code{x} is a pre-built mapping
(the mapping already encodes its own separator).}

\item{excluded_cols}{Columns to leave untouched (only meaningful when
\code{x} is a mapping; when \code{x} is an XLSForm, the same vector is passed
through to \code{\link[=build_sm_label_map]{build_sm_label_map()}}).}

\item{choice_label}{Forwarded to \code{\link[=build_sm_label_map]{build_sm_label_map()}} when \code{x} is an
XLSForm. Ignored otherwise.}

\item{tool_flavor}{Forwarded to \code{\link[=build_sm_label_map]{build_sm_label_map()}} when \code{x} is an
XLSForm path. Ignored otherwise.}
}
\value{
The data frame with renamed columns.
}
\description{
Renames the raw \code{Question_code} columns in a dataset to their labeled
equivalent (e.g. \code{fruits_3} -> \code{fruits/Banana}). The second argument is
polymorphic:
}
\details{
\itemize{
\item Pass a mapping data frame produced by \code{\link[=build_sm_label_map]{build_sm_label_map()}}
when you want explicit control over how labels are constructed.
\item Pass an XLSForm (path / \code{\link[=read_xlsform]{read_xlsform()}} result / pre-read
\code{survey} data frame) when you just want the labels applied with
defaults - the function builds the map internally.
}
}
