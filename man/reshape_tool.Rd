% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape_tool.R
\name{reshape_tool}
\alias{reshape_tool}
\title{Reshape an XLSForm into a select_multiple column-name mapping}
\usage{
reshape_tool(
  tool,
  excluded_cols = "",
  choice_label = NULL,
  tool_flavor = "auto"
)
}
\arguments{
\item{tool}{Path to the XLSForm, or a pre-built result from \code{\link[=read_xlsform]{read_xlsform()}}.}

\item{excluded_cols}{Vector of question names to skip.}

\item{choice_label}{Column name in the \code{choices} sheet that holds the choice
label. If \code{NULL} (the default), the function tries the typical XLSForm
names (\code{label::English}, \code{label:English}, \code{label}) and picks the first
one that exists.}

\item{tool_flavor}{One of \code{"auto"}, \code{"kobo"}, \code{"surveycto"}. Only used when
\code{tool} is a file path. See \code{\link[=read_xlsform]{read_xlsform()}}. Default \code{"auto"}.}
}
\value{
Data frame with columns \code{Question}, \code{type}, \code{select_type},
\code{response_code}, \code{response_label}, \code{response_label_new}, \code{dataset_col},
\code{labeled_col}.
}
\description{
Reads an XLSForm and returns a data frame that maps each raw dataset column
(e.g. \code{q1_3}) to its labeled equivalent (e.g. \code{q1/Banana}). Useful for
renaming series columns of \code{select_multiple} questions to human-readable
labels. Works with both Kobo and SurveyCTO XLSForms.
}
