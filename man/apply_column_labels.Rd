% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_column_labels.R
\name{apply_column_labels}
\alias{apply_column_labels}
\title{Rename data columns to use survey labels (all question types)}
\usage{
apply_column_labels(
  data,
  tool,
  survey_label = NULL,
  choice_label = NULL,
  sm_label_separator = ".",
  custom_labels = NULL,
  excluded_cols = "",
  tool_flavor = "auto"
)
}
\arguments{
\item{data}{A data frame.}

\item{tool}{Path to the XLSForm, a \code{\link[=read_xlsform]{read_xlsform()}} result list, or a
pre-read \code{survey}-sheet data frame. Note: applying choice labels to
\code{select_multiple} series columns requires a full XLSForm (path or
list); a bare survey data frame is enough for everything else.}

\item{survey_label}{Column name in the \code{survey} sheet that holds the
question label. If \code{NULL} (default), the function picks
\code{label::English}, \code{label:English}, or \code{label} - whichever exists.}

\item{choice_label}{Column name in the \code{choices} sheet that holds the
choice label. Same auto-pick rules as \code{survey_label}. Only used when
the XLSForm has a choices sheet.}

\item{sm_label_separator}{Separator placed between the (labeled) question
prefix and the sanitized choice label for \code{select_multiple} series
columns. Default \code{"."}.}

\item{custom_labels}{Optional. Names + labels for columns that don't
appear in the XLSForm (typically calculated or derived variables).
Accepts:
\itemize{
\item A named character vector: \code{c(my_calc = "My calculation")}.
\item A named list with character entries.
\item A data frame with columns \code{name} and \code{label}.
}
Labels are sanitized the same way as survey labels.}

\item{excluded_cols}{Columns to leave untouched.}

\item{tool_flavor}{One of \code{"auto"}, \code{"kobo"}, \code{"surveycto"}. Only used
when \code{tool} is a file path. Default \code{"auto"}.}
}
\value{
The data frame with renamed columns.
}
\description{
Walks the XLSForm and renames every column in \code{data} whose name matches a
question in the \code{survey} sheet to a sanitized version of the question's
label. For \code{select_multiple} questions the parent column is renamed via
the survey label and the series columns are renamed via the choice labels
(e.g. \code{fruits_1} -> \code{Pick_fruits.Apple}). Columns whose name is not in
the XLSForm can be renamed via the optional \code{custom_labels} argument -
useful for calculated / derived variables added during analysis.
}
\details{
Priority when more than one source could rename a given column:
\code{custom_labels} > select_multiple choice labels > survey label.
}
